function custom_ajax_before(){
  //var inputs = element.select("input[type=submit][disabled=true][data-disable-with]");
  //inputs.each(function(input) {
  //  input.value = input.readAttribute('data-disable-with');
  //  input.disabled = true;
  //});
  alert('hey');
}